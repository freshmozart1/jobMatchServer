# cosine-similarity is a transitive (non-root) git dependency pulled in by
# cover-letter-generator, so allow-git=root is not sufficient here — it only
# permits git deps declared directly in this project's own package.json.
allow-git=all
